 
<%= form_with model: [@user, @post], local: true do |form| %>
  <div class="container">
  <div class="field"> 
  <%= form.label :title %>
  <%= form.text_field :title %>
  </div>

  <div class="field"> 
  <%= form.label :text %>
  <%= form.text_area :text %>
  </div>

  <div>
  <%= form.submit "Create Post" %>
  </div>

  <div>
<% end %>